-- local formatters = require("lvim.lsp.null-ls.formatters")
-- formatters.setup({
--     { command = "rome", filetypes = { "javascript", "typescript", "json" } },
-- })

require("lspconfig").rome.setup({})
